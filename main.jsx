import React from "react";
import ReactDOM from "react-dom/client";
import InveryoungSimulator from "./Simulador.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InveryoungSimulator />
  </React.StrictMode>
);
